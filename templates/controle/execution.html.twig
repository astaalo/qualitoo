{% form_theme form 'OrangeMainBundle:Extra:simple_form_theme.html.twig' %}

<div class="header">
	<img src="{{ asset('assets/bundles/orangemain/images/icon/color_18/checkmark.png') }}" alt="add">
	<span>Test du contrôle</span>
	<div class="close_me"><a data-dismiss="modal"><img src="{{ asset('assets/bundles/orangemain/images/icon/closeme.png') }}"> </a></div>
</div>
<div class="contentForm">
	<form  novalidate="novalidate"  action="{{ path('tester_controle', {id: entity.id}) }}" method="POST">
		{{ form_row(form.valide) }}
		{{ form_row(form.file) }}
		{{ form_row(form.commentaire) }}
		{{ form_row(form._token) }}
		<div class="section last">
			<div>
				<button data-target="#myModal" class="uibutton loading submitLink" call="redirectModalToUrl">Enregistrer</button>
				<input type="reset" data-dismiss="modal" class="uibutton special" value="Effacer" >
			</div>
		</div>
	</form>
</div>
