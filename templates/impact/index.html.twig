{% extends 'configuration/index.html.twig' %}

{% block menu_config %}
	{% include 'configuration/evaluation.html.twig' with {position: 1} %}
{% endblock %}

{% block header_config %}
<div class="header">
	<span>
		<span class="ico color gear"></span>Liste des impacts
	</span>
	<div id="buttom_top">
		<ul class="uibutton-group">
			<li><a class="uibutton icon filter actionLink" href="#myModal" modal-url="{{ path('filtrer_les_impacts') }}" data-toggle="modal" data-target="#myModal">Filtrer</a></li>
			<li><a class="uibutton icon download" href="{{ path('exporter_les_impacts') }}">Extraire</a></li>
        </ul>
    </div>
</div>
{% endblock %}

{% block content_config %}
<div class="content_table">
	<div class="tableName">
		<table class="display simple_table" id="impactTable">
			<thead>
				<tr>
					<th><div class="th_wrapp">Nom du risque</div></th>
					<th><div class="th_wrapp">Nom de l'impact</div></th>
					<th><div class="th_wrapp">Critère</div></th>
					<th><div class="th_wrapp">Dernière évaluation</div></th>
					<th width="60px"><div class="th_wrapp">Actions</div></th>
				</tr>
			</thead>
			<tbody>
			 	<tr class="odd gradeX">
					<td colspan="5">Chargement des données en cours ...</td>
				</tr>
			</tbody>
		</table>
	</div>
</div>
{% endblock %}

{% block javascripts %}
	{{ parent() }}
	{# {{ block_render('::form_div_layout.html.twig', 'datatable_options', {has_buttons: true, with_tag: 1 }) }} #}
	{% include 'extra/datatable.html.twig' with {idTable: 'impactTable', url: path('liste_des_impacts'), nosorting: {0:2, 1:3} } %}
{% endblock %}
